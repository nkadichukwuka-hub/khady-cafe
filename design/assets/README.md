# Source design assets

Original, editable files. Do not delete after export.

- `logo/` — logo lockups (SVG/AI/Figma exports)
- `icons/` — source icon set
- `photos/` — full-res photography
- `mockups/` — high-fidelity page comps

Shipping assets (optimized) live in `../public/`.
